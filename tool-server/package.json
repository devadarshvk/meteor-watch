{
  "name": "meteor-tool-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ZAP tool server for the Meteor Debris Watch domain.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs"
  }
}
